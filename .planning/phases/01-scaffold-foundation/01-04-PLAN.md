---
plan: 01-04
phase: 1
title: "Scroll-Snap Layout Shell + Footer"
wave: 3
depends_on: [01-02]
files_modified:
  - src/app/page.tsx
  - src/components/layout/Footer.tsx
  - src/components/layout/Footer.module.css
  - src/components/layout/SectionDots.tsx
  - src/components/layout/SectionDots.module.css
  - src/app/layout.tsx
autonomous: true
requirements: [SCAF-04, SCAF-05, SCAF-06, NAV-01]
---

# Plan 01-04: Scroll-Snap Layout Shell + Footer

## Goal
Implement the scroll-snap container on the homepage with 7 placeholder sections, section navigation dots, and the Footer component. This establishes the complete layout skeleton that Phase 2 will fill with real content.

## Context
- Scroll snap contract: defined in `01-UI-SPEC.md` → `globals.css` already has `.snap-container` and `.snap-section` classes
- Section IDs must exactly match what the Navbar links target: `hero`, `problem`, `how-it-works`, `features`, `dashboard-teaser`, `about`, `contact`
- Section dots: fixed right side, 8px circles, hidden on mobile

## Tasks

<task id="01-04-A">
<title>Build Homepage with scroll-snap sections</title>
<action>
Replace `src/app/page.tsx` with the homepage scroll-snap layout. Each section is a placeholder that Phase 2 will fill with real content.

```tsx
import styles from "./page.module.css";

const SECTIONS = [
  {
    id: "hero",
    label: "Hero",
    bg: "var(--color-bg)",
    title: "Know What's In Your Mess",
    subtitle: "AI-powered food & water quality monitoring for college canteens",
  },
  {
    id: "problem",
    label: "Problem",
    bg: "var(--color-surface-alt)",
    title: "The Problem",
    subtitle: "Food safety in college canteens is invisible — until someone gets sick",
  },
  {
    id: "how-it-works",
    label: "How It Works",
    bg: "var(--color-bg)",
    title: "How CLEANMess Works",
    subtitle: "Sense → Analyze → Alert",
  },
  {
    id: "features",
    label: "Features",
    bg: "var(--color-surface-alt)",
    title: "Features & Technology",
    subtitle: "Everything you need to monitor and trust your canteen",
  },
  {
    id: "dashboard-teaser",
    label: "Dashboard Preview",
    bg: "var(--color-bg)",
    title: "Live Data Dashboard",
    subtitle: "See historical sensor trends — real data, real insights",
  },
  {
    id: "about",
    label: "About",
    bg: "var(--color-surface-alt)",
    title: "About CLEANMess",
    subtitle: "Born from a real problem in college hostels",
  },
  {
    id: "contact",
    label: "Contact",
    bg: "var(--color-bg)",
    title: "Get In Touch",
    subtitle: "Interested in CLEANMess for your institution?",
  },
] as const;

export default function HomePage() {
  return (
    <div className="snap-container" id="snap-container">
      {SECTIONS.map((section) => (
        <section
          key={section.id}
          id={section.id}
          className={`snap-section ${styles.section}`}
          style={{ backgroundColor: section.bg }}
          aria-label={section.label}
        >
          <div className={styles.sectionInner}>
            <p className={styles.sectionLabel}>{section.label}</p>
            <h2 className={styles.sectionTitle}>{section.title}</h2>
            <p className={styles.sectionSubtitle}>{section.subtitle}</p>
            <p className={styles.placeholder}>
              — Content coming in Phase 2 —
            </p>
          </div>
        </section>
      ))}
    </div>
  );
}
```

Create the companion CSS module `src/app/page.module.css`:

```css
.section {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: var(--space-4xl) var(--space-lg);
}

.sectionInner {
  max-width: var(--content-max-width);
  width: 100%;
  text-align: center;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: var(--space-md);
}

.sectionLabel {
  font-size: var(--text-xs);
  font-weight: var(--weight-semibold);
  color: var(--color-primary);
  text-transform: uppercase;
  letter-spacing: 0.08em;
}

.sectionTitle {
  font-size: var(--text-h2);
  font-weight: var(--weight-bold);
  color: var(--color-text);
  line-height: 1.2;
}

.sectionSubtitle {
  font-size: var(--text-lg);
  color: var(--color-text-muted);
  max-width: 600px;
}

.placeholder {
  font-size: var(--text-sm);
  color: var(--color-text-disabled);
  margin-top: var(--space-xl);
  padding: var(--space-md) var(--space-xl);
  border: 1px dashed var(--color-border);
  border-radius: var(--radius-md);
}
```
</action>
<read_first>
- src/app/page.tsx (current auto-generated content — will be replaced)
- src/styles/globals.css (snap-container and snap-section class names)
- .planning/ROADMAP.md (section order and IDs)
</read_first>
<acceptance_criteria>
- `src/app/page.tsx` exists with 7 `<section>` elements
- Each section has `class="snap-section"` and the correct `id` attribute
- IDs present: `hero`, `problem`, `how-it-works`, `features`, `dashboard-teaser`, `about`, `contact`
- `src/app/page.module.css` exists with `.section`, `.sectionTitle`, `.sectionLabel` classes
- Sections wrap in a `div` with `class="snap-container"` and `id="snap-container"`
- Every section alternates background colors (bg, surface-alt, bg, surface-alt, ...)
- TypeScript: `npx tsc --noEmit` exits 0
</acceptance_criteria>
</task>

<task id="01-04-B">
<title>Create SectionDots navigation indicator</title>
<action>
Create `src/components/layout/SectionDots.tsx` — a Client Component that shows which section is active via small dot indicators on the right side of the viewport.

Create `src/components/layout/SectionDots.module.css`:
```css
.dots {
  position: fixed;
  right: var(--space-lg);
  top: 50%;
  transform: translateY(-50%);
  display: flex;
  flex-direction: column;
  gap: var(--space-sm);
  z-index: 50;
}

.dot {
  width: 8px;
  height: 8px;
  border-radius: var(--radius-full);
  background-color: var(--color-border-strong);
  cursor: pointer;
  transition: background-color var(--transition-base), transform var(--transition-base), width var(--transition-base);
  position: relative;
  border: none;
  padding: 0;
}

.dot:hover {
  background-color: var(--color-primary);
}

.dot.active {
  background-color: var(--color-primary);
  transform: scale(1.4);
}

/* Tooltip */
.dot::before {
  content: attr(data-label);
  position: absolute;
  right: calc(100% + 10px);
  top: 50%;
  transform: translateY(-50%);
  background-color: var(--color-text);
  color: var(--color-surface);
  font-size: var(--text-xs);
  font-weight: var(--weight-medium);
  padding: var(--space-xs) var(--space-sm);
  border-radius: var(--radius-sm);
  white-space: nowrap;
  opacity: 0;
  pointer-events: none;
  transition: opacity var(--transition-fast);
}

.dot:hover::before {
  opacity: 1;
}

@media (max-width: 768px) {
  .dots {
    display: none;
  }
}
```

Create `src/components/layout/SectionDots.tsx`:
```tsx
"use client";

import { useEffect, useState } from "react";
import styles from "./SectionDots.module.css";

const SECTIONS = [
  { id: "hero", label: "Home" },
  { id: "problem", label: "Problem" },
  { id: "how-it-works", label: "How It Works" },
  { id: "features", label: "Features" },
  { id: "dashboard-teaser", label: "Dashboard" },
  { id: "about", label: "About" },
  { id: "contact", label: "Contact" },
];

export default function SectionDots() {
  const [activeSection, setActiveSection] = useState("hero");

  useEffect(() => {
    const snapContainer = document.getElementById("snap-container");
    if (!snapContainer) return;

    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            setActiveSection(entry.target.id);
          }
        });
      },
      {
        root: snapContainer,
        rootMargin: "-40% 0px -40% 0px",
        threshold: 0,
      }
    );

    SECTIONS.forEach(({ id }) => {
      const el = document.getElementById(id);
      if (el) observer.observe(el);
    });

    return () => observer.disconnect();
  }, []);

  const scrollToSection = (id: string) => {
    const el = document.getElementById(id);
    if (el) el.scrollIntoView({ behavior: "smooth" });
  };

  return (
    <nav className={styles.dots} aria-label="Section navigation">
      {SECTIONS.map(({ id, label }) => (
        <button
          key={id}
          className={`${styles.dot} ${activeSection === id ? styles.active : ""}`}
          onClick={() => scrollToSection(id)}
          data-label={label}
          aria-label={`Navigate to ${label} section`}
          aria-current={activeSection === id ? "true" : undefined}
        />
      ))}
    </nav>
  );
}
```
</action>
<read_first>
- src/app/page.tsx (section IDs to match)
- .planning/phases/01-scaffold-foundation/01-UI-SPEC.md (Section Navigation Dots spec)
</read_first>
<acceptance_criteria>
- `src/components/layout/SectionDots.tsx` exists and starts with `"use client"`
- `src/components/layout/SectionDots.module.css` exists
- SECTIONS array contains all 7 section IDs matching `page.tsx`
- CSS has `@media (max-width: 768px)` with `display: none`
- Each dot button has `aria-label` and `data-label` attributes
- Tooltip (`.dot::before`) uses CSS `content: attr(data-label)`
</acceptance_criteria>
</task>

<task id="01-04-C">
<title>Create Footer component</title>
<action>
Create `src/components/layout/Footer.module.css`:
```css
.footer {
  background-color: var(--color-surface);
  border-top: 1px solid var(--color-border);
  padding: var(--space-xl) var(--space-lg);
}

.inner {
  max-width: var(--site-max-width);
  margin: 0 auto;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: var(--space-lg);
  flex-wrap: wrap;
}

.brand {
  font-size: var(--text-base);
  font-weight: var(--weight-bold);
  color: var(--color-primary);
}

.tagline {
  font-size: var(--text-sm);
  color: var(--color-text-muted);
  margin-top: var(--space-xs);
}

.copy {
  font-size: var(--text-sm);
  color: var(--color-text-disabled);
  text-align: right;
}

@media (max-width: 600px) {
  .inner {
    flex-direction: column;
    align-items: flex-start;
    text-align: left;
  }
  .copy {
    text-align: left;
  }
}
```

Create `src/components/layout/Footer.tsx`:
```tsx
import styles from "./Footer.module.css";

export default function Footer() {
  const year = new Date().getFullYear();
  return (
    <footer className={styles.footer} role="contentinfo">
      <div className={styles.inner}>
        <div>
          <p className={styles.brand}>CLEANMess</p>
          <p className={styles.tagline}>IoT + AI food safety for college canteens</p>
        </div>
        <p className={styles.copy}>
          © {year} CLEANMess. Built for safer meals.
        </p>
      </div>
    </footer>
  );
}
```
</action>
<read_first>
- src/styles/tokens.css (CSS variable names for spacing and color)
</read_first>
<acceptance_criteria>
- `src/components/layout/Footer.tsx` exists (Server Component — no `"use client"`)
- `src/components/layout/Footer.module.css` exists
- Footer renders `<footer role="contentinfo">`
- Footer contains "CLEANMess" brand text
- CSS uses only CSS variables for all color and spacing values
</acceptance_criteria>
</task>

<task id="01-04-D">
<title>Wire SectionDots and Footer into layout</title>
<action>
Update `src/app/layout.tsx` to include SectionDots (rendered client-side) and Footer (after main content on non-snap pages). 

Since SectionDots only makes sense on the homepage, render it inside the layout but note it uses IntersectionObserver on sections that exist only on page.tsx. It will silently not observe anything on other pages if sections aren't present — that's correct behavior.

Update layout.tsx:
```tsx
import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "@/styles/tokens.css";
import "@/styles/globals.css";
import Navbar from "@/components/layout/Navbar";
import SectionDots from "@/components/layout/SectionDots";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});

export const metadata: Metadata = {
  title: "CLEANMess — Know What's In Your Mess",
  description:
    "AI-powered food and water quality monitoring for college canteen safety. Real-time sensor alerts, historical trends, instant incident reporting.",
  keywords: ["food safety", "IoT", "canteen monitoring", "college mess", "water quality"],
  openGraph: {
    title: "CLEANMess — Know What's In Your Mess",
    description:
      "AI-powered food and water quality monitoring for college canteen safety.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className={inter.variable}>
      <body>
        <Navbar />
        <SectionDots />
        <main>{children}</main>
      </body>
    </html>
  );
}
```
</action>
<read_first>
- src/app/layout.tsx (current content)
- src/components/layout/SectionDots.tsx (just created — confirm import path)
</read_first>
<acceptance_criteria>
- `src/app/layout.tsx` imports both `Navbar` and `SectionDots`
- `<SectionDots />` renders in the layout
- `npm run dev` → browser at localhost:3000 shows:
  - Fixed navbar at top
  - 7 dot indicators on right side of viewport (desktop)
  - 7 placeholder sections that snap on scroll
  - Dots hide at ≤ 768px
- Scrolling between sections — active dot highlights in green (`--color-primary`)
- No console errors
</acceptance_criteria>
</task>

## Verification

Full integration test after `npm run dev`:

```
1. Visit http://localhost:3000
2. Page loads — navbar visible at top, 7 dots visible on right
3. Scroll down — sections snap into place
4. Active dot updates to match current section (green highlight)
5. Click a dot — page snaps to that section
6. Hover a dot — tooltip shows section name
7. Resize to 375px — dots disappear, hamburger appears
8. Click hamburger — mobile menu opens with 7 nav items
```

## Must-Haves (for goal verification)
- [ ] 7 scroll-snap sections with correct IDs exist on homepage
- [ ] Sections snap cleanly (no partial positions) using `scroll-snap-type: y mandatory`
- [ ] `100dvh` used (not `100vh`) so iOS Safari doesn't clip content
- [ ] Section dots appear on desktop, hide on mobile
- [ ] Active dot updates on scroll via IntersectionObserver
- [ ] Footer renders correctly (not inside snap container)
- [ ] `scroll-padding-top: 64px` prevents navbar overlap on anchor jump
